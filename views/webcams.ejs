<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Webcams</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light text-center">

<div class="container py-5">

    <a href="/profile" 
    class="float-end text-decoration-none text-muted fw-bold link-primary">
        <%= usernameData.username %>
    </a>
    <%-include('partials/header.ejs') %>

    <h1 class="mb-4">Webcams</h1>

    <% if (webcams.webcams.length == 0) { %>
        <div class="alert alert-warning">No webcams found for the selected filters.</div>
    <% } else { %>
        <div class="row row-cols-1 row-cols-sm-2 row-cols-md-3 g-4">
            <% webcams.webcams.forEach(webcam => { %>
                <div class="col">
                    <div class="card shadow-sm">
                        <img src="<%= webcam.images.current.thumbnail %>" class="card-img-top" alt="Thumbnail for <%= webcam.title %>">
                        <div class="card-body">
                            <h5 class="card-title"><%= webcam.title %></h5>
                            <p class="card-text">View Count: <%= webcam.viewCount %></p>
                            <p class="card-text"><small class="text-muted">Status: <%= webcam.status %></small></p>
                            <p class="card-text">[ <a href="/displayCam?webcamId=<%= webcam.webcamId %>"><b>View Webcam</b></a> ]</p>
                        </div>
                    </div>
                </div>
            <% }); %>
        </div>
    <% } %>
</div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
